Dans le répertoire : TME810, on ouvre le terminal.
On va ensuite entrer ces commandes :

mkdir build
cd build
cmake ../src
make install
./tme810


On pourra charger le fulladder en fesant :

shortcut : CTRL+O
on écrit : fulladder
ensuite on click sur ok

